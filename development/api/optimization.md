# Optimization API (Not Implemented)

The current codebase does not expose `/api/optimization` routes.

For parameter search at this time, use:

- `/api/backtest/batch` for small sets of symbols
- `/api/queue/bulk-backtest` for larger bulk runs

If/when optimization endpoints are added, this document should be updated to reflect the actual routes and request models.
