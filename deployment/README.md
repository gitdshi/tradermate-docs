# Deployment Docs

This directory contains production deployment templates and historical operational docs. The main code repo only ships `docker-compose.dev.yml` for local development; production deployments must be adapted per environment.

## Current (Templates)

- `DEPLOYMENT.md` - production deployment template (high-level)
- `docker-compose.prod.yml` - example compose file (template)
